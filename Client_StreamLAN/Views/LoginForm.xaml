<Window x:Class="Client_StreamLAN.Views.LoginForm" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:Client_StreamLAN.Views" mc:Ignorable="d" Title="StreamLAN Login" Height="550" Width="450" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize" Background="Transparent">
    <Window.Resources>
        <!-- Autumn PasswordBox Style -->
        <Style x:Key="AutumnPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="{StaticResource AutumnTextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="#D2B48C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AutumnAccentBrush}" />
                                <Setter TargetName="border" Property="BorderThickness" Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Window Control Button Style -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AutumnAccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Close Button Style -->
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AutumnSecondaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <!-- Outer border -->
    <Border Background="{StaticResource AutumnWindowBackground}" BorderBrush="{StaticResource AutumnAccentBrush}" BorderThickness="1" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect Color="#2E1A11" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <!-- Background Gradient removed (using Border's background) -->
            <Grid.Background>
                <SolidColorBrush Color="Transparent" />
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#3E2723" CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="35">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- App Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                        <TextBlock Text="🔐 " FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="StreamLAN Login" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <!-- Window Controls -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="−" Click="MinimizeButton_Click" Style="{StaticResource WindowControlButton}"/>
                        <Button Content="✕" Click="CloseButton_Click" Style="{StaticResource CloseButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- Login Form Content -->
            <Grid Grid.Row="1" Margin="40,30">
                <StackPanel VerticalAlignment="Center" MaxWidth="350">
                    <!-- Logo/Title Section -->
                    <TextBlock Text="🍁" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/> <!-- Changed icon to leaf -->
                    <TextBlock Text="Welcome Back" Style="{StaticResource AutumnTitleText}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Sign in to continue" Style="{StaticResource AutumnBodyText}" HorizontalAlignment="Center" Margin="0,0,0,40"/>
                    <TextBlock Text="Email Address" Style="{StaticResource AutumnBodyText}" Margin="0,0,0,5"/>
                    <!-- Email Field -->
                    <TextBox x:Name="txt_Email" Style="{StaticResource AutumnTextBox}" Height="40" Margin="0,0,0,20"/>
                    <!-- Password Field -->
                    <TextBlock Text="Password" Style="{StaticResource AutumnBodyText}" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txt_Password" Style="{StaticResource AutumnPasswordBox}" Height="40" Margin="0,0,0,30"/>
                    <!-- Login Button -->
                    <Button Content="Sign In" Click="Login_Btn_Click" Style="{StaticResource AutumnPrimaryButton}" Height="48"/>
                    <!-- Footer -->
                    <TextBlock Text="StreamLAN Video Streaming" FontSize="11" Foreground="{StaticResource AutumnTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,30,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>