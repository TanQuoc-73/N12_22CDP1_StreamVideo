<Window x:Class="Server_StreamLAN.Views.LoginForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Server Login" Height="300" Width="420" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="{StaticResource AutumnWindowBackground}" BorderBrush="{StaticResource AutumnAccentBrush}" BorderThickness="1" CornerRadius="10">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Server Admin Login" Style="{StaticResource AutumnTitleText}" HorizontalAlignment="Center"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" Width="320">
                <TextBlock Text="Username" Style="{StaticResource AutumnBodyText}"/>
                <TextBox x:Name="txt_Username" Style="{StaticResource AutumnTextBox}" Height="36"/>
                <TextBlock Text="Password" Style="{StaticResource AutumnBodyText}" Margin="0,8,0,0"/>
                <PasswordBox x:Name="txt_Password" Height="36"/>
                <Button x:Name="btnSignIn" Content="Sign In" Style="{StaticResource AutumnPrimaryButton}" Height="40" Margin="0,12,0,0"/>
                <Button x:Name="btnGoogle" Content="Sign in with Google" Style="{StaticResource AutumnSecondaryButton}" Height="40" Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
