<Window x:Class="Server_StreamLAN.Views.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:Server_StreamLAN.Views" mc:Ignorable="d" Title="StreamLAN Server" Height="650" Width="1000" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResize" Background="Transparent">
    <Window.Resources>
        <!-- Window Control Button Style -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AutumnAccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Close Button Style (Red hover) -->
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AutumnSecondaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <!-- Outer border for window appearance -->
    <Border Background="{StaticResource AutumnWindowBackground}" BorderBrush="{StaticResource AutumnAccentBrush}" BorderThickness="1" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect Color="#2E1A11" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <!-- Background Gradient removed (using AutumnWindowBackground on Border) -->
            <Grid.Background>
                <SolidColorBrush Color="Transparent" />
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#3E2723" CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="35">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- App Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                        <TextBlock Text="🖥️ " FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="StreamLAN Server" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <!-- Window Controls -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="−" Click="MinimizeButton_Click" Style="{StaticResource WindowControlButton}"/>
                        <Button x:Name="MaximizeButton" Content="□" Click="MaximizeButton_Click" Style="{StaticResource WindowControlButton}"/>
                        <Button Content="✕" Click="CloseButton_Click" Style="{StaticResource CloseButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="20,10,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- Header Section -->
                <Border Grid.Row="0" Style="{StaticResource AutumnCardPanel}">
                    <StackPanel>
                        <TextBlock Text="StreamLAN Server" Style="{StaticResource AutumnTitleText}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🌐 " FontSize="14" Foreground="#94a3b8"/>
                            <TextBlock Text="Server IP: " Style="{StaticResource AutumnBodyText}"/>
                            <TextBlock x:Name="txtLocalIp" Text="" Style="{StaticResource AutumnStatusText}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Ellipse Width="12" Height="12" Fill="{StaticResource AutumnSecondaryBrush}" Margin="0,0,10,0"/>
                            <TextBlock Text="Listening on port 9000 (Video) and 9001 (Discovery)" Style="{StaticResource AutumnBodyText}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <!-- Camera Feed Section -->
                <Border Grid.Row="1" Style="{StaticResource AutumnCardPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <!-- Camera Feed Title -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                            <Ellipse Width="12" Height="12" Fill="{StaticResource AutumnAccentBrush}" Margin="0,0,10,0"/>
                            <TextBlock Text="Incoming Stream" Style="{StaticResource AutumnHeaderText}"/>
                        </StackPanel>
                        <!-- Camera Display -->
                        <Border Grid.Row="1" Style="{StaticResource AutumnVideoContainer}">
                            <Image x:Name="imgCamera" Stretch="Uniform" Margin="5"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>